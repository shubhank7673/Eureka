<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>create quiz</title>
    <link rel="stylesheet" href="/css/teacher/createQuiz.css">
</head>
<style>
    .input-div{
        display: flex;
        flex-direction:column ;
        /* justify-content: center; */
        /* text-align: center; */
        align-items: center;
        /* border: 1px solid black; */
        /* height: calc(70vh - 55px); */
    }
    .input-div input{
        border: none;
        font-size: 23px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.18);
        outline: none;
        padding: 15px;
        margin: 20px 10px;
        border-radius: 15px;
    }
    ::placeholder{
        opacity: 0.4;
    }
    .add-problems-btn{
        /* width: 40vw; */
        font-size: 20px;
        padding: 13px 15px ;
        color: white;
        background: #665effff;
        border: none;
        border-radius: 10px;
        margin-top: 20px;
    }
</style>
<body>
    <div class="top">
            <img onclick="window.history.back()" src="/images/left-arrow.svg" class="left-arrow" alt="">
            <h2>Create Quiz</h2>
    </div>
    <form action="/teacher/quiz/createQuiz?classId=<%= classId %>" method="POST">
        <div class="input-div">
            <input type="text" style="margin-top: 10vh;" placeholder="Quiz title" name="title" id="">
            <input type="number" placeholder="Quiz duration(seconds)" name="duration" id="">
            <button class="add-problems-btn">Add problems</button>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        </div>
    </form>
</body>
</html>